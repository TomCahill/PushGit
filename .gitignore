.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# WebdriverIO E2E run logs — written fresh by every `npm run test:e2e`
/logs

.private

# Personal/local Claude Code overrides — not shared with the team
CLAUDE.local.md